{
  "name": "saucedemo_cypress",
  "version": "1.0.0",
  "description": "Saucedemo E-commerce Cypress test automation",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/cypress run --spec 'cypress/integration/tests/*.js'",
    "head_test": "npm run test -- --headed",
    "chrome_test": "npm run test -- --headed --browser chrome",
    "parallel_test": "npm run test -- --headed --browser chrome --parallel"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^7.7.0"
  },
  "devDependencies": {
    "cypress-mochawesome-reporter": "^2.2.0",
    "mochawesome": "^6.2.2",
    "mochawesome-merge": "^4.2.0"
  }
}
